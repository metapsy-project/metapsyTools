# Fit 'waap-wls' model

Fit 'waap-wls' model

## Usage

``` r
fitWaapWlsModel(
  which.run,
  which.waap.wls,
  mGeneral,
  mComb,
  beta.within.study,
  method.tau,
  .raw.bin.es,
  .type.es,
  round.digits,
  nnt.cer,
  rob.data
)
```
