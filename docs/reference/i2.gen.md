# Compute I-squared from tau2 using the "generalized" formula (as used, e.g., in metafor). This formula uses an estimate of the "typical" within-study variance (v-tilde).

Compute I-squared from tau2 using the "generalized" formula (as used,
e.g., in metafor). This formula uses an estimate of the "typical"
within-study variance (v-tilde).

## Usage

``` r
i2.gen(tau2, k, vi)
```
