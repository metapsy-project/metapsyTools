# Fit 'outliers' model

Fit 'outliers' model

## Usage

``` r
fitOutliersModel(
  data,
  study.var,
  multi.study,
  mGeneral,
  .type.es,
  round.digits,
  .raw.bin.es,
  nntCer,
  which.run,
  which.outliers,
  method.tau,
  m.for.outliers,
  rob.data
)
```
