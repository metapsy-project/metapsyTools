# Flagging engine for internal use

Flagging engine for internal use

## Usage

``` r
flagStudy(
  smd,
  se,
  rob,
  power = "q1",
  pval = 0.05,
  reference = c("all", "dep", "psy", "ptsd"),
  ...
)
```
