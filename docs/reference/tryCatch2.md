# `tryCatch` alternative that saves the error message

`tryCatch` alternative that saves the error message

Try function and catch errors

## Usage

``` r
tryCatch2(expr)

tryCatch2(expr)
```

## Arguments

- expr:

  R expression.
