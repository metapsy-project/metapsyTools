# Fit 'influence' model

Fit 'influence' model

## Usage

``` r
fitInfluenceModel(
  which.influence,
  mComb,
  mGeneral,
  which.run,
  method.tau,
  .raw.bin.es,
  .type.es,
  round.digits,
  nntCer,
  rob.data
)
```
