# Fit 'overall' model

Fit 'overall' model

## Usage

``` r
fitOverallModel(
  data,
  es.var,
  se.var,
  arm.var.1,
  arm.var.2,
  measure.var,
  study.var,
  .raw.bin.es,
  .type.es,
  hakn,
  method.tau.meta,
  method.tau.ci,
  method.tau,
  dots,
  es.binary.raw.vars,
  round.digits,
  nntCer,
  which.run,
  rob.data
)
```
